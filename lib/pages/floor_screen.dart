import 'package:flutter/material.dart';

class FloorPage extends StatelessWidget {
  const FloorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("Welcome to floor page"),
      ),
    );
  }
}
